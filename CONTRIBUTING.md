# Contributing

Please follow these guidelines when contributing:

- Use feature branches and create PRs against `main`.
- Commit messages should follow Conventional Commits (example: `feat(api): add quotes endpoint`).
- Run linters and tests before opening a PR.
- Ensure CI passes.

Branch protection: require a PR review and passing CI before merge.

-- H2-compatible adjustments (runs only under 'dev' profile alongside V1)
-- H2 in PostgreSQL mode supports most of V1; this file is intentionally empty
-- and exists so the H2 migration locations array resolves cleanly.
SELECT 1;

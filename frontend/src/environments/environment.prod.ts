export const environment = {
  production: true,
  // Absolute URL so the prod build works when served by a plain static server
  // (no Nginx reverse-proxy). Change to '/api' when behind Nginx.
  apiUrl: 'http://localhost:8080/api',
  whatsappNumber: '919505056386',
  gstNumber: '36ABCDE1234F1Z5',
};
